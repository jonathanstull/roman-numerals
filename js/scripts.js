let numbersInput = [1];
let numeralKey = ["I"];
let numerals = [];

function romanize() {
  for (i = 0; i < numbersInput.length; i += 1) {
    if (numbersInput[0] === 1) {
      return numerals = numeralKey.slice();
    };
  };
};